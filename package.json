{
  "name": "resource-modules",
  "version": "1.0.14",
  "description": "",
  "main": "dist/cli.js",
  "bin": {
    "resource-modules": "./dist/cli.js"
  },
  "scripts": {
    "start": "tsc && node dist/cli.js",
    "test": "tsc && cp -r src/test/fixtures dist/test/ && tape \"dist/test/**/*.js\" | tap-spec",
    "dist": "rm -rf dist/ && tsc && chmod +x dist/cli.js && rm -rf dist/test"
  },
  "files": [
    "dist/",
    "src/php/resources.php"
  ],
  "author": "Joaquin Oltra <joaquin@chimeces.com> (http://chimeces.com)",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^6.0.41",
    "tap-spec": "^4.1.1",
    "tape": "^4.6.0",
    "typescript": "^2.0.3"
  },
  "dependencies": {
    "acorn": "^3.3.0",
    "deep-equal": "^1.0.1",
    "escodegen": "^1.8.1",
    "recursive-readdir": "^2.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/joakin/resource-modules.git"
  }
}
