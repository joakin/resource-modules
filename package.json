{
  "name": "resource-modules",
  "version": "1.0.10",
  "description": "",
  "main": "dist/cli.js",
  "bin": {
    "resource-modules": "./dist/cli.js"
  },
  "scripts": {
    "start": "npm run compile && node dist/cli.js",
    "compile": "rm -rf dist/ && tsc",
    "test": "npm run compile && cp -r src/test/fixtures dist/test/ && tape \"dist/test/**/*.js\" | tap-spec",
    "dist": "npm run compile && chmod +x dist/cli.js && rm -rf dist/test"
  },
  "files": [
    "dist/",
    "resources.php"
  ],
  "author": "Joaquin Oltra <joaquin@chimeces.com> (http://chimeces.com)",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^6.0.41",
    "tap-spec": "^4.1.1",
    "tape": "^4.6.0",
    "typescript": "^2.0.3"
  },
  "dependencies": {
    "acorn": "^3.3.0",
    "deep-equal": "^1.0.1",
    "escodegen": "^1.8.1",
    "recursive-readdir": "^2.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/joakin/resource-modules.git"
  }
}
